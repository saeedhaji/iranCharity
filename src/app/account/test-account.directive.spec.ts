import { TestAccountDirective } from './test-account.directive';

describe('TestAccountDirective', () => {
  it('should create an instance', () => {
    const directive = new TestAccountDirective();
    expect(directive).toBeTruthy();
  });
});
